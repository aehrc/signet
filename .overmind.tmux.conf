# Intentionally empty. Overmind runs each Procfile process in a tmux window,
# and a user-level ~/.tmux.conf that sets a global PATH (e.g. for launchd)
# strips bun from every window after the first. Pointing overmind at this
# file keeps the caller's environment intact.
