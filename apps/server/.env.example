# Signet's configuration: every variable the server itself reads.
#
# Bun loads `.env` files from the working directory and does not search upwards, so
# this file is what `bun run --filter @signet/server dev` and
# `cd apps/server && bun run test` read - both run here. Commands run from the
# repository root read the root `.env.example` instead.
#
# Copy everything below into `.env.local`, except the last variable, which goes in
# `.env.test`: `bun test` sets NODE_ENV=test, and Bun does not load `.env.local` in
# that mode - it loads `.env` and `.env.test`. A test variable left in `.env.local`
# is silently absent, and the integration suites here skip themselves while the run
# reports success.
#
# This is also the reference for a deployment. A container, a Helm release and a
# `bun run dev` all read the same names; the difference is where the values come
# from, not what they are called. See docs/operations.md.

# Public origin Signet is reached on. Endpoint issuers are derived from it as
# {SIGNET_PUBLIC_URL}/t/{tenant}/e/{endpoint}, so changing it invalidates every
# issuer already published to FHIR servers and registered clients.
SIGNET_PUBLIC_URL=http://localhost:3000

PORT=3000
SIGNET_LOG_LEVEL=info

# The serving role: what the server and `bootstrap` connect as.
#
# It must own none of Signet's tables. Postgres exempts a table's owner from that
# table's row-level security policies, so a server connecting as the owner is not
# constrained by the tenant isolation those policies provide - and the server checks
# this at startup and refuses to serve when the role it is given is exempt.
#
# Either a full connection URL...
SIGNET_DATABASE_URL=postgres://signet_app:signet_app@localhost:5432/signet

# ...or discrete parts, which is what the Helm chart uses with the bundled
# PostgreSQL subchart so it never has to invent a password. Set one or the
# other, not both.
# SIGNET_DATABASE_HOST=localhost
# SIGNET_DATABASE_PORT=5432
# SIGNET_DATABASE_NAME=signet
# SIGNET_DATABASE_USER=signet_app
# SIGNET_DATABASE_PASSWORD=signet_app

# The owning identity, read by `migrate` and by nothing else. Migrations are DDL,
# and the grants that follow them must be issued by the role that owns the objects
# being granted - so this credential belongs in the migration step, not in the
# running server's environment. `migrate` reads SIGNET_DATABASE_URL as well, for
# the serving role's name alone: it grants that role its access and never uses its
# password. Two URLs naming the same role are refused.
SIGNET_DATABASE_OWNER_URL=postgres://signet:signet@localhost:5432/signet

# Envelope key that encrypts endpoint signing keys at rest. At least 32
# characters. Losing this makes every stored signing key undecryptable, so back
# it up somewhere other than the database it protects.
SIGNET_MASTER_KEY=change-me-0123456789abcdef0123456789abcdef

# Directory holding the built UI. Set in the container image; leave unset in
# development, where Vite serves the UI and proxies /api to this server.
# SIGNET_WEB_ROOT=/app/web

# Turns off the outbound-fetch guard, permitting private addresses and plain HTTP
# when Signet fetches a client's jwks_uri or an upstream IdP's discovery document.
# Needed only for a development or connectathon stack whose IdP is on a compose
# network. Never set this in production: an authorization server with an SSRF sits
# inside the network holding the FHIR servers it fronts.
# SIGNET_ALLOW_PRIVATE_OUTBOUND_FETCHES=false

# Read only by `bootstrap`, which creates the first tenant and its owner. Nothing
# else in the server looks at them, and the running server never does.
SIGNET_BOOTSTRAP_TENANT=dev
SIGNET_BOOTSTRAP_TENANT_NAME=Development
SIGNET_BOOTSTRAP_EMAIL=you@example.org
SIGNET_BOOTSTRAP_PASSWORD=change-me
SIGNET_BOOTSTRAP_NAME=Developer

# --- .env.test, not .env.local -----------------------------------------------
# The throwaway database this package's integration suites may migrate, create roles
# in and truncate. Read by `bun run --filter @signet/server test` and by
# `cd apps/server && bun run test`; the root `bun run test` reads the root
# `.env.test`. Leaving it unset skips those suites rather than failing them, so check
# for a `skip` count if you are not sure it is being read.
SIGNET_TEST_DATABASE_URL=postgres://signet:signet@localhost:5432/signet
